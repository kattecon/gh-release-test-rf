# Test-subject of type 'release-artifacts' for gh-release

A test repository that is used by [gh-release](https://github.com/kattecon/gh-release)'s test workflow to test 'release-artifacts' release workflow.
